# 电商履约：完整链路验收计划

> 状态：商城主链路及关键故障闭环已通过，完整扩展计划尚未全部验收。制定日期：2026-09-26；制定时源码基线：`cac9236c92cf3a0550d047bddf0d6fe19481af9a`。
>
> 用户已追加授权本地后端部署与 Codex 内置浏览器验证。执行使用专用 MySQL/Redis 容器、独立端口与数据卷；不改动现有 Windows MySQL，也不视为云端或生产部署授权。实际通过项以新增验收记录为准。
>
> 本计划的目标是形成“能启动、能从页面完成交易、最终数据正确、关键失败可恢复、另一轮可以复现”的验收证据。计划不是已经通过的验收报告，也不预先承诺生产可用。

首轮执行进展（2026-09-26）：本地隔离部署、145 项后端测试、三条真实浏览器交易闭环、66 项 API 断言及 38 项小规模并发断言通过，详见[首轮报告](docs/local-browser-acceptance-2026-09-26.md)。同日后续已完成 R03/R04 与购物车并发清理，252 项后端测试及 52 条故障断言通过，R05 部分验证、R06 仍保留；完整 P0/P1 尚未全部通过，详见[可靠性专项](docs/reliability-recovery-2026-09-26.md)和[可执行入口](scripts/acceptance/README.md)。

最新收尾：270 项后端与 15 项前端测试通过；真实库存竞争两批复验、F04/F06/F07、安全负面用例及恢复专项通过。正常直连部署再跑 66 条 API 与 38 条并发断言通过；全库只读核验 139 单全部终态、11 条支付事件全部 SENT、无活跃库存锁或守恒差异。证据见[闭环补验](docs/closure-acceptance-2026-09-26.md)。以下未执行的 Redis 独立专项、B04 和多实例等仍保留，不将本次收尾等同于整份扩展计划通过。

## 1. 先确定验收范围和完成标准

主验收对象：Vue 前端 + Gateway、Commerce、Order、Inventory、Payment 五个后端进程，以及真实 MySQL、Redis。以当前消费者商城入口为主线，不用根目录单体接口冒充新版商城。

完整业务闭环拆成三笔独立订单：

```text
注册/登录 → 商品列表 → 商品详情 → 选择 SKU → 购物车 → 结算 → 订单详情
                                                               ├─ 订单 A：模拟支付 → 库存确认
                                                               ├─ 订单 B：主动取消 → 库存释放
                                                               └─ 订单 C：到期未支付 → 自动取消 → 库存释放
```

每条链路必须同时满足：浏览器可操作、API 结果正确、数据库最终状态正确。不能只看到 HTTP 200 或页面提示成功就判通过。

分级结论：

| 等级 | 内容 | 可以宣布什么 |
| --- | --- | --- |
| P0：基础闭环与安全正确性 | 可复现启动、构建测试、三条商城链路、身份/金额边界、重复请求、基本并发一致性 | 全部通过后，可以说“本机商城基础闭环通过” |
| P1：已有可靠性能力 | 支付事件、取消补偿、Redis 独立异步入口、结果未知、重启恢复、对账和积压可观测 | P0、P1 的必测项均通过后，才可以说“本计划范围内完整链路验收通过” |
| P2：生产化与容量扩展 | 多实例、生产容量、真实支付、备份恢复、密钥治理、完整告警 | 另行验收；不能由 P0/P1 自动推出 |

不纳入当前“完整”的业务：真实扣款、发货、物流、退款、售后和营销。项目没有这些现成闭环，本轮不凭空扩建。

## 2. 执行前发现的问题与处理门槛

下表保留制定计划时的问题背景，不是修复后的当前实现清单。R01/R02 已修复并通过针对性验证，R07 的本地隔离启动缺口已补齐。同日后续专项已补齐并实测 R03/R04；R05 增加 owner/lease 与审计重驱实现，普通失败重试、真实死信重驱与确认后崩溃恢复已通过，多实例仍未验收；R06 保持隔离限制。详见[可靠性专项](docs/reliability-recovery-2026-09-26.md)及[闭环补验](docs/closure-acceptance-2026-09-26.md)。

历史证据：[V2 MVP 验收](docs/mvp-v2-test-evidence-2026-09-24.md)、[V2.1 订单操作验收](docs/v2-order-actions-evidence-2026-09-24.md)。它们记录了后端 HTTP 联调和构建，但明确没有完整浏览器逐页验收及多实例故障验证。本次必须重新形成证据，不能直接引用历史“通过”。

| 编号 | 发现与证据 | 处理门槛 |
| --- | --- | --- |
| R01 | [普通公开创建](microservices/order-service/src/main/java/com/why/fulfillment/order/web/OrderController.java) 与 [Redis 公开创建](microservices/order-service/src/main/java/com/why/fulfillment/order/web/RedisOrderController.java) 仍直接接受请求体用户、总额和单价；[网关](microservices/gateway/src/main/resources/application.yml) 将这些入口路由给登录用户，未强制经过 Commerce 商品校验 | P0 安全阻断项。先在隔离环境复现，再封闭消费者绕行入口或补可信身份/价格来源；不能以旧压测兼容为由放行 |
| R02 | [发号器](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/service/OrderIdGenerator.java) 生成 Long；[结算响应](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/dto/CheckoutResultView.java) 返回 Long；[前端](frontend/src/api/http.js) 直接 JSON.parse。按当前公式构造的样例 `48559973990400001` 被解析为 `48559973990400000` | P0 正确性阻断风险。内存计算已证明可丢精度，但真实浏览器链路尚待复现。订单号须在响应、URL、支付/取消请求与数据库中精确一致 |
| R03 | [结算服务](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/service/CheckoutService.java) 与 [记录 Mapper](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/mapper/CheckoutRequestMapper.java)：结果未知时保留处理中，缺少可靠的事前订单关联与自动恢复任务 | P1 恢复前置。健康主链可先验，但“未知后自动恢复”不能带缺口通过 |
| R04 | [普通订单编排](microservices/order-service/src/main/java/com/why/fulfillment/order/service/OrderApplicationService.java) 提交订单意图后远程预占；[到期扫描查询](microservices/order-service/src/main/java/com/why/fulfillment/order/repository/OrderRepository.java) 只处理已预占订单，普通创建停在 RESERVING 时没有统一后台恢复 | P1 恢复前置。设计并验证安全恢复，不能靠测试人工重发请求假装自动收敛 |
| R05 | [支付事件发布器](microservices/order-service/src/main/java/com/why/fulfillment/order/task/InventoryConfirmationPublisher.java) 有重试与死信，但没有完整的安全重驱入口；其领取也没有 Redis 命令那样的 owner 隔离 | P1 必须有明确的受控处置与复验办法；多实例与陈旧执行者隔离属于 P2 前置，不能声称已验证 |
| R06 | 普通库存预占只写 MySQL；[Redis 快速入口](microservices/order-service/src/main/java/com/why/fulfillment/order/service/RedisOrderApplicationService.java) 另行预扣。初始化后的同 SKU 混合流量不能假设天然一致 | P1 单独核验。默认商城普通路径与快速实验路径隔离；如果宣称同 SKU 可混用，必须修复并通过混合一致性验证 |
| R07 | [Compose](docker-compose.yml) 只提供依赖，不包含完整商城启动/微服务迁移；[启动手册](docs/mvp-v2-runbook-2026-09-24.md) 有字段名、环境变量及全新环境步骤缺漏；前端目前没有测试脚本 | P0 环境与复现前置。补齐实际可执行步骤，不把“机器上曾经跑过”当成复现能力 |

制定计划时，R01 仅为源码可见的信任边界问题，R02 仅由源码分析加纯内存计算证明风险；执行后已增加旧入口拒绝测试及真实浏览器订单号核验，见当轮报告。其余恢复缺口仍需专项场景确认和修复。

原定修复方向（下列保留原计划；R01–R04 当前执行结果见专项报告）：

- R01：消费者创建统一走 Commerce；旧实验入口仅在隔离实验配置/受保护边界开放。涉及旧 API 行为变化时先记录兼容方案，不直接删除基线能力。
- R02：对外订单标识采用不丢精度的契约并同步调用方与测试；不能在数字已经被 JSON.parse 舍入后再简单转字符串。
- R03/R04：先保存足以定位业务的关联事实，定义成功、取消、未知的恢复规则及并发条件，再补任务；不能通过无限盲重试或无条件回补“解决”。
- 全部按“复现 → 最小修复 → 对应回归 → 完整主链复跑”推进，不先重构整个项目。

## 3. 数据与运行安全约束

默认使用专用验收 MySQL/Redis 实例及独立端口、数据卷、账号；可以保留代码约定的 schema 名，但不得连接现有演示实例。若无法隔离，应先停在准备阶段，由用户确认备份和复用边界，不能擅自重建当前数据库。

执行前须记录：

- Git 提交和本地差异；保留现有 `tutorial.md`、导学和面经，不把它们清理掉。
- 依赖版本、端口、数据源目标、Redis 目标、服务 URL、调度参数；密钥只记录“已配置/一致”，不输出值。
- 数据库初始化顺序、最小权限账号、商品与库存 SKU 对应关系；迁移需同时覆盖新库和已有库，不直接重复执行破坏性初始化。
- 本批账号、SKU、幂等键、订单标识、用例 ID；每个用例先记录库存基线。
- 本轮创建的服务 PID/容器身份、日志位置、停止与恢复方法。

明确禁止：

- 清空共享 Redis、删除现有数据库、重置既有演示商品库存。
- 为结束进程而停止所有 Java/Node/MySQL/Redis 进程。
- 在复验前删除失败现场，或直接改库存把对账结果“修到通过”。
- 在命令输出、截图、验收报告、Git 中保存密码、JWT 或内部令牌。
- 在本轮计划阶段执行迁移、故障注入、压力测试或业务修复。

## 4. 分阶段执行路线

| 阶段 | 要做的工作 | 输出与放行条件 |
| --- | --- | --- |
| S0：冻结基线与隔离 | 核对环境、初始化依赖链、数据归属、配置与端口；建立本次专用数据 | 环境清单和安全边界明确；无法确认隔离就不进入写入阶段 |
| S1：构建与可复现启动 | 执行微服务测试、前端锁文件安装与构建；补预检/启动/停止脚本；启动五服务与前端 | 当前测试结果可记录；依赖和应用健康；浏览器实际能请求商品，不只看进程存在 |
| S2：接口闭环与关键阻断 | 先复现 R01/R02；按优先级修复并回归；完成支付、取消、超时三分支 API 验收 | 三条链路最终数据正确，身份与金额信任边界无绕行 |
| S3：浏览器端到端 | 从页面注册到三笔订单分别支付/取消/超时，检查刷新、错误、登录切换与精确订单号 | 页面、网络请求、数据库三方一致；无关键操作必须绕到命令行才能完成 |
| S4：负面与并发正确性 | 重复请求、异载荷、改价、无货、多 SKU 回滚、归属、支付与取消竞争、热点库存 | 无超卖、重复扣减、部分预占、越权和错误成功提示 |
| S5：故障与异步恢复 | R03/R04 恢复修复；验证服务中断、支付事件、取消补偿、Redis 命令、账本和对账 | 限时收敛或受控失败处置可复验；已有承诺的能力不能以“已知缺陷”冒充通过 |
| S6：复跑与交付 | 汇总指标及证据；重启自己创建的应用后重复执行核心套件；同步运行文档与现状规则 | 冷启动一轮 + 保留本批数据重启后再跑一轮均可复现；形成明确验收结论与剩余边界 |

阶段失败时记录阻塞与证据，优先修复相关原因，再重跑受影响用例。不要把后面的“通过”用于覆盖前面的数据错误。

## 5. 环境和启动验收清单

计划逐项核对 Java 17、Maven、Node、pnpm、MySQL 8、Redis 和可用的依赖启动方式。工具命令存在不等于服务就绪；数据库连通也不等于迁移和权限正确。

- 后端分别核验 Gateway、Order、Inventory、Payment、Commerce，不能漏掉模拟支付所需的 Payment。
- JWT 签发与验签使用一致配置；Gateway、Feign、Order、Inventory、Payment 的内部令牌一致。
- 三个业务数据源及最小权限账号可用；Payment 回调密钥单独配置。
- Commerce 和 Inventory 都使用 `SERVER_PORT`，若改端口必须给各子进程独立配置，不能用一个全局值覆盖二者。
- MySQL 的库存列是 `stock / lock_stock`，不照抄旧手册中的 `total_stock / reserved_stock`。
- 全新数据库不能只执行 V2 的三条增量 SQL；需梳理当前迁移对旧库与前序表的依赖。
- 种子数据中商品 SKU 与库存 SKU 一致；账号通过注册流程产生，不提交真实账号凭证。
- 前端依赖以锁文件为准；当前 `package.json` 只有开发、构建、预览，没有现成前端端到端测试套件。
- 前端使用开发代理或 Nginx 代理到正确 Gateway。若验收构建产物，应明确其 API 代理，不能将仅能渲染静态页面视为可用商城。
- 根目录单体另用专用环境，不能与微服务共用同一批库存事实或一起消费任务。

本机实际可用性在执行 S0 时重新确认；本计划不依赖历史工具安装记录作为现时运行保证。

## 6. 三条核心链路：接口和最终断言

统一前置：专用用户 A/B；不同分支使用独立 SKU 或明确分批库存基线。下面以每分支初始可售库存 10、锁定 0、购买 3 件作测试夹具示例，不是历史实测值。

共同操作：

1. 匿名浏览 `GET /api/products` 和 `GET /api/products/{spuId}`。
2. 注册/登录：`POST /api/auth/register`、`POST /api/auth/login`，验证刷新后身份仍可用于受保护接口。
3. 加购：`POST /api/cart/items`，请求 `{skuId, quantity}`；修改数量、勾选、删除均通过真实接口。
4. 读取购物车，使用服务端 `selectedAmount`；保留一条未勾选商品验证不会被误清理。
5. `POST /api/checkout`：携带 `Idempotency-Key`，请求仅包含 `expectedAmount`；用户和商品售价不由浏览器指定。
6. 查询本人订单列表/详情，核对商品快照、单价、数量、总额、归属及精确订单号。商品分页从 1 开始，订单分页从 0 开始，测试脚本不能混用。

| 用例 | 关键操作 | 最终订单/事件 | 最终库存 |
| --- | --- | --- | --- |
| B01 支付闭环 | 下单后 `POST /api/payments/orders/{id}/mock-success`，随后重复支付 | 订单 PAID，预占仍可为 RESERVED；交易号稳定；一份有效支付事件最终已发送；后续取消被拒绝 | 可售 7、锁定 0；本订单库存锁 CONFIRMED；重复调用不再消费 |
| B02 主动取消 | 下单后 `POST /api/orders/{id}/cancel`，随后重复取消与尝试支付 | 订单 CANCELED + COMPENSATED；重复取消无副作用；支付被拒绝 | 可售恢复 10、锁定 0；库存锁 RELEASED |
| B03 超时关单 | 仅在隔离环境把 Commerce 下单超时配置缩短，不传入虚构的前端超时字段；等待订单真实到期 | 到期后 CANCELED + COMPENSATED；已支付的 B01 不受扫描影响 | 可售恢复 10、锁定 0；库存锁 RELEASED |
| B04 商品快照 | 已创建订单后通过受控夹具改商品价格/名称，再查订单 | 历史订单的约定价格、名称和规格不被商品改动覆盖 | 不产生额外库存变化 |
| B05 成功后重放 | 使用原幂等键、原预期金额重放已清车的结算 | 返回原订单号，不新增订单；最新订单状态另从订单查询确认 | 不新增预占，不将结算结果的旧状态误当当前状态 |

普通商城路径不要求产生 Redis 预扣账本，也不把“Redis 有值”作为普通下单完成条件。

## 7. 浏览器验收专项

必须通过真实页面操作，不能用 API 成功替代：

- 商品列表、详情、购物车、登录、订单列表、订单详情六个页面均可进入。
- 注册后登录、退出后再登录、页面刷新、受保护路由跳转正常。
- 加购、改数量、勾选与删除会反映后端结果，未勾选项目不被结算误清。
- 至少连续创建三笔独立订单，分别支付、取消和超时；逐笔对比原始 JSON、数据库 ID、URL 和后续请求 ID，防止第一笔恰好无舍入而漏检。
- 页面上的订单状态与后端一致；当前没有自动轮询，应刷新核对，不要求尚未实现的实时推送。
- 双击提交与网络错误时不自动产生新的结算键造成重复单；结果未知不能直接展示成功或引导无限换键重下。
- 切换 A/B 账号，检查购物车与订单隔离；浏览器残留缓存/幂等键不能造成另一用户交易混淆。
- 请求失败或令牌失效有可理解提示；无阻断操作的前端异常。
- 保存必要的页面截图和脱敏请求摘要，不能将完整凭证导出到公开证据。

## 8. 负面与并发正确性矩阵

| 用例 | 场景 | 放行条件 |
| --- | --- | --- |
| N01 | 无登录、过期/篡改 JWT，伪造用户头 | 受保护操作拒绝；不能创建订单或修改他人数据 |
| N02 | 用户 A 查询、取消、支付 B 的订单；直连业务端口伪造用户头 | 归属校验有效；缺内部令牌的直连请求拒绝；数据库无副作用 |
| N03 | 已登录用户调用旧创建入口，伪造用户、价格、总额 | 默认消费者边界应拒绝或采用可信数据；不能接受任意报价/代他人建单 |
| N04 | 看购物车后价格改变、商品下架、非法数量 | 返回确定错误；没有新订单或库存预占；刷新并确认后才允许新结算 |
| N05 | 同用户同键串行与并发重复；同键改载荷 | 同内容只形成一笔业务订单，异内容拒绝；处理中不误报成功 |
| N06 | 查询时有货、预占时已被抢完 | 真正库存更新拒绝；无部分预占；不能只验前置库存查询 |
| N07 | 多 SKU 中一项足量、一项不足；相反输入顺序 | 全部预占成功或整体回滚；重复 SKU 规则符合所在入口；无未解释的残留锁 |
| N08 | 同交易号重复、同号用于不同订单、同单改交易号；错误/过期支付签名 | 合法重放幂等，冲突/无效签名拒绝，支付与库存不多变一次 |
| C01 | 专用 SKU 库存 10，至少 30 次不同业务请求竞争 | 成功预占数量不超过 10；成功/业务拒绝/基础设施异常分别计数；不得把异常算售罄 |
| C02 | 同订单支付与主动取消并发，支付贴近到期时间 | 只允许一个订单决策获胜；绝无“已支付且库存被释放” |
| C03 | 已完成订单反复支付/取消/确认/释放 | 无重复扣减或回补，历史快照不被重写 |

并发先小规模验证正确性，再逐级提高；当前机器资源不明时不直接跑大压力。根目录的超卖/死锁测试作为独立回归，测试通过也不能替代以上新版微服务用例。

## 9. 故障、Redis 异步和对账矩阵

故障只作用于本次专用进程/依赖或受控请求窗口，先写明恢复步骤；不得停用用户的共享服务。

| 用例 | 注入或操作 | 必须核对 |
| --- | --- | --- |
| F01 | 库存预占可能提交后响应丢失 | 结果未知不被当作未扣；订单、锁和补偿事实最终可解释，无孤立占用 |
| F02 | 普通订单意图落库后、预占结果写回前重启 Order | 恢复策略自动识别未完成订单并安全收敛；当前 R04 必须先补齐 |
| F03 | 下游创建成功但 Commerce 丢响应/重启 | 原幂等键关联原订单并安全恢复；不生成第二张单；当前 R03 必须先补齐 |
| F04 | 取消时 Inventory 不可用，随后恢复 | 待补偿持久化，恢复后库存完整释放；期间页面不声称已释放 |
| F05 | 支付提交后库存确认失败，随后恢复 | 已支付和事件保留；最终只确认一次，事件进入已发送 |
| F06 | 库存已确认、事件未标已发时中断发布器 | 陈旧领取可恢复；重复投递不重复消费 |
| F07 | 持续失败直至事件死信 | 死信可见、原因可查；按受控处置流程重驱并最终确认。仅检测到死信不等于交易完成 |
| A01 | 独立 Redis 入口同号同载荷重放、同号异载荷、某 SKU 无货 | 先持久化命令；Lua 全量校验后扣减；无重复/部分预扣；202 仅算已受理 |
| A02 | Redis 执行结果未知，或预扣成功但账本写入失败 | 命令留有事实并重试；准备阶段放弃前须完成幂等墓碑补偿，晚到请求不能再扣 |
| A03 | 命令获得租约后执行进程退出 | 租约过期可重新领取；旧 owner 不能错误提交；只有一笔有效订单 |
| A04 | 订单已创建、账本物化暂时失败 | 仅重试物化，不回补有效订单；物化成功后命令才成功 |
| A05 | Redis 订单落库后主动取消/超时 | MySQL、Redis、锁记录和账本幂等收敛；重复补偿不再加库存 |
| A06 | 同一 SKU 先普通交易再快速交易，以及反向顺序 | 专项检查两路径混合兼容；不允许每步先重置 Redis 掩盖差异。若当前明确隔离运行，此项记录“不支持混用”，不宣称支持 |
| O01 | 正常在途、静止、缺失键、损坏账本及受控差异 | 只读对账能区分在途与错误；不会自动改数；已启用快速路径的本批 SKU 静止后无未解释差异 |
| O02 | 制造少量积压后恢复，检查业务指标 | 状态数、最老年龄及对账结果与事实相符；旧指标不被当成新鲜结果；对账须单独触发 |

异步快速入口按实验边界单独验收，不为了宣称“所有技术用上”临时接入购物车主线。其安全隔离必须与 R01 的消费者入口收敛方案一致。

## 10. 等待上限与失败分类

下列是计划中的验收观察预算，不是已有生产 SLO：

- 健康环境的普通下单、支付确认、主动取消：最多观察 30 秒。
- 超时关单：从订单实际到期时刻起，再观察最多 30 秒。
- 依赖恢复后的补偿/陈旧领取/命令租约：依据下一次调度与租约时间观察；常规预算 90 秒，不在租约尚未到期时误判。
- 如果任务已经进入较长退避，以持久化的下一次可执行时间 + 30 秒为观察点，常规故障场景总预算上限 6 分钟。
- 完整死信上限另测：按当前退避公式预留最多 15 分钟或使用可控时间测试环境；不能偷偷改业务记录跳过关键流程，不能和 30 秒冒烟共用判断。
- 到上限仍未收敛，保留现场、判失败或阻塞，不无限等待直到偶然通过。

报告统一使用 PASS / FAIL / BLOCKED / NOT_RUN；不适用项目必须说明限定范围。某次故障能被检测到，不等于后续业务已经恢复；两项应分别记录。

## 11. 数据不变量与证据格式

在专用 SKU 无补货、无人工调整的测试批次中：

```text
初始可售库存 + 初始锁定库存
    = 当前可售库存 + 当前锁定库存 + 本批已确认消耗量
```

同时核对：

- 可售/锁定库存不得为负，成功订单数量与实际数量变化相匹配。
- 已支付订单最终对应确认的库存锁；已取消订单最终没有残留锁定。
- 同一订单与 SKU 不重复预占、确认或释放；失败订单没有部分成功残留。
- 同一个有效支付业务不新增重复事件；投递次数可以超过一次，业务效果不能超过一次。
- Redis 期望可售 = MySQL 可售 − Inventory 自有 PENDING 账本预扣量；只对启用相应路径且基线明确的 SKU 使用该断言。
- HTTP 202、处理中和死信不算正常业务终态。

每个用例留存：用例 ID、提交版本、配置摘要、开始/结束时间、脱敏请求与响应、关联订单号、前后数据库只读快照、必要 Redis 状态、日志摘要、页面证据和判定。订单号始终按精确字符串保存，测试工具本身不能先丢精度。

验证工具可用独立只读凭证分别检查各 schema；这不授权业务服务跨库读取。清理必须在保存证据后，严格按本批明确目标执行。

## 12. 计划交付物

以下是计划交付清单；启动、接口回归、浏览器证据和主链报告现已落地，可靠性专项仍待补齐，不能视为全部完成：

1. 环境预检、启动和停止入口：缺配置即明确报错，等待健康，记录本轮 PID；不自动打印密钥。
2. 专用验收环境初始化与种子说明：明确全新安装和增量升级路径、数据归属与可恢复清理。
3. 可重复执行的 API 验收套件：正向、负面、幂等、并发及最终数据库断言。
4. 浏览器端到端用例与截图证据；核心页面流程可复跑。
5. 故障/恢复专项套件：可控故障、明确恢复、限时观察，不依赖手工改库存。
6. 当轮验收报告：用例结果、修复记录、提交版本、运行边界、未通过项目及复现步骤。
7. 同步 README、运行手册、功能边界和 AGENTS 的现状描述；保留旧周期证据，不覆盖历史记录。
8. 保留教学文件，仅在实现事实变化影响教学时做针对性同步，不借验收大改无关材料。

正式执行时建议先完成 S0—S3，让用户得到一套确实能从页面走通的商城；然后完成 S4—S6，补齐可重复和失败恢复能力。若只完成前半段，报告必须写“基础闭环通过、可靠性验收未完成”，不能提前宣称本计划全部完成。

本轮已按用户追加授权进入执行阶段。专用数据库初始化、必要修复、后端启动和浏览器主链验证已完成；后续故障与恢复专项继续按以上边界推进，不扩大为生产保证。

前一轮执行进展：252 项 Maven 测试、52 条真实故障断言通过；66 条 API/38 条并发断言复验通过。F02 覆盖预占前与预占后中断，F03 覆盖丢响应后 Commerce 重启，F05 覆盖 503 后恢复；新增购物车改量/删后重加/ABA 的真实并发保护。内置浏览器验证改车并刷新后仍返回原单、原金额。同日后续 F04/F06/F07、安全负面用例与浏览器账号切换已补验，详见[最新报告](docs/closure-acceptance-2026-09-26.md)。Redis 专项、多实例及 B04 的改名改价后历史快照核查仍 NOT_RUN，完整计划仍不能判定通过。
