# 高并发电商平台：项目面经

> 初稿源码基线：`cac9236c92cf3a0550d047bddf0d6fe19481af9a`；整理日期：2026-09-26。面向 Java 后端岗位。同日按可靠性修复更新口播与源码索引，真实测试范围见验收报告；不将历史单体压测视作本轮重跑。
>
> 使用边界：个人实际负责、独立实现、AI 辅助与协作范围均待本人确认。下文第一人称是“理解和讲解”的口播参考，不证明本人执行过全部改造或实验；不能直接升级为“我独立设计、我主导上线”。仅在理解并核对个人经历后用于面试。S/T/A/R 分别表示情境、任务、分析动作和结果/边界。

## 1. 项目简介

基于 Java、Spring Boot、MySQL 和 Redis 构建前后端分离的电商演示平台，覆盖商品浏览、购物车、下单、库存预占、模拟支付和订单取消。后端重点处理并发库存、重复请求、支付与关单竞争以及跨服务失败恢复，当前完成本机微服务交易链路验证，尚非生产高可用系统。

## 2. 简历候选要点

先提炼五个支柱：业务建模、并发一致性、请求可靠性、异步恢复、验证与观测。以下是项目事实候选，不是五条全部塞入简历的建议；个人职责确认后，优先选最能独立解释的两到三条。

- **业务建模：** 针对原有履约实验缺少消费者入口的问题，项目扩展商品、购物车和结算链路，服务端重新校验价格并持久化订单快照，使商品到模拟支付形成可验证的业务闭环；个人负责模块待确认，真实支付单尚未实现。
- **并发一致性：** 针对热点库存竞争和多商品反向加锁问题，项目通过条件更新、库存锁记录与统一加锁顺序约束扣减；历史单体对照实验验证了防超卖和限定场景下的死锁改善，不能外推为任意负载下无冲突；个人实施与实验职责待确认。
- **请求可靠性：** 针对重复结算与支付回调，项目分别使用用户维度幂等键、订单载荷校验、交易号约束和条件状态迁移，形成可重放且不重复推进的处理边界；结算未知结果仍有人工核对缺口，个人负责范围待确认。
- **异步恢复：** 针对跨服务确认失败和快速受理后的落库风险，项目持久化支付事件与异步命令，并按阶段执行幂等重试或补偿，使处理过程留下可追踪事实；不同任务恢复能力不一，不能承诺全部故障自动收敛，个人实施范围待确认。
- **验证与观测：** 针对只看接口成功难以发现最终状态异常的问题，项目结合真实数据库实验、HTTP 联调、只读对账和积压指标验证结果；历史压测明确区分同步提交与异步受理，新版商城容量仍待测，个人测试与观测职责待确认。

## 3. 面试问题与口播参考

以下按准备优先级从高到低排列，并非招聘市场频率的统计结论。共 15 个主问题，分 5 组；每组另有 2 个追问。正文优先使用通用工程语言，具体文件与方法统一放在第 4 节证据索引。

### 第 1 组：项目定位与交易闭环（最高优先）

#### Q01｜请介绍这个项目，它主要解决什么问题？

口播：S（情境）我的理解是，这个项目从订单库存实验逐步扩展成了一个前后端分离的电商演示平台，不能只用技术栈列表介绍。T（任务）我会把核心问题概括成用户能够完成交易，同时后台在库存竞争、重复请求和跨服务失败时保持可解释的状态。A（动作）我会先讲商品浏览、购物车、结算、订单、模拟支付和取消的业务闭环，再选择库存条件更新、请求幂等和异步确认三个机制展开；同时区分新版商城与保留的单体实验基线，避免把两条路径混在一起。R（结果）仓库已有本机交易链路和并发对照记录，可以支撑这些机制的介绍，但不能据此声称真实线上流量或生产高可用。我的实际开发范围还需要按个人经历确认，不把整套仓库能力都说成个人独立完成。

#### Q02｜用户点击结算后，一次普通商城下单经过哪些环节？

口播：S（情境）用户在购物车点结算时，页面上的金额和库存只是此前查询的结果，并不意味着订单已经成立。T（任务）我会沿着一次请求解释，系统怎样重新确认交易内容并把订单和库存状态连接起来。A（动作）前端带着本次操作的幂等键和预期总额进入网关，商城服务读取当前用户勾选的商品，重新检查上下架、价格和数量，固定快照后认领结算记录；随后订单服务在自己的事务中保存订单及明细，再通过远程接口请求库存服务预占，并根据返回结果推进订单状态。R（结果）当前实现成功后记录结算结果并清理已结算购物车，未知结果则保留处理中并提示核对订单。这里走的是普通数据库下单主线，不是默认经过 Redis 异步入口，也不能把整条调用链解释成同一个事务。

#### Q03｜为什么这样划分服务，事务边界在哪里？

口播：S（情境）商城入口需要组织用户、商品和购物车，而订单状态、库存变化、支付回调各自有不同的正确性要求。T（任务）我会先说明数据由谁负责，再解释为什么远程调用不能依赖一个本地事务兜底。A（动作）我的理解是，商城服务负责商品事实和结算快照，订单服务负责订单与支付状态，库存服务负责可售及锁定库存，支付服务负责回调校验和转交支付结果。订单与明细可以在订单服务内一起提交，库存预占则由库存服务独立提交；跨边界失败需要记录状态、幂等重试或补偿，而不是期待调用方回滚远端数据库。R（结果）当前代码形成了进程、数据所有权和本地事务边界，但业务数据库仍共用一个数据库实例，支付侧也没有独立持久化支付单。因此我会称它为本地微服务运行切片，而不是已完成物理资源隔离的生产架构。

#### F01｜追问：既然交易链路已经跑通，为什么还不能称为生产系统？

口播：S（情境）演示环境能够完成下单和模拟支付，只能证明被验证条件下的业务路径可以工作，并不能覆盖真实运营中的全部风险。T（任务）我需要明确“功能跑通”和“生产就绪”的差别，避免用架构名词替代运行证据。A（动作）我会从业务、安全和运维三个方面回答：业务上还没有真实支付渠道和完整支付单；安全上共享令牌与登录校验不等于网络隔离、密钥轮换和完整授权；运维上尚未完成多实例竞争、容量验收、自动容灾以及完整告警体系。此外，结算结果未知和部分死信仍需要人工核对或恢复。R（结果）仓库已有记录支持本机链路、限定并发实验和部分失败处理，不能支持可用性承诺或线上容量数字。我的表达会保留这些限制，把后续需要验证的工作与已经实现的能力明确分开。

#### F02｜追问：为什么用户、商品和购物车不继续拆成独立服务？

口播：S（情境）当前商城业务规模有限，用户、商品与购物车经常在同一次浏览或结算中协作，继续拆分会增加远程调用和部署成本。T（任务）我的理解是，服务边界应服务于业务和运行需求，而不是为了展示更多微服务进程。A（动作）我会把现有选择解释为先在一个商城服务内部按模块组织职责：商品模块提供价格与上下架事实，购物车记录用户选择，身份模块提供当前用户；对外仍通过清晰接口连接订单和库存。这样既能形成交易闭环，也能让学习和排障先集中在真正存在的跨服务一致性问题上。R（结果）当前实现采用了这种模块化结构，但没有通过生产流量证明它是最优划分。如果未来某个模块出现独立扩容、独立发布或团队所有权需求，我会先补指标和契约，再评估拆分，不把更多进程直接等同于更高性能。

### 第 2 组：业务数据与身份可信（高频）

#### Q04｜为什么结算时要重新校验商品，并保存订单价格快照？

口播：S（情境）用户打开购物车后可能停留很久，期间商品可能改价或下架，其他订单也可能消耗库存。T（任务）我需要解释系统怎样防止按陈旧信息下单，同时让历史订单不受后续商品变化影响。A（动作）我会先区分展示和交易事实：购物车保存商品选择，展示时再查询价格；结算时服务端重新读取商品和规格，校验状态与数量，用当前价格计算总额，再与用户提交的预期金额比较。这个金额只用于确认双方看到的总额一致，不允许客户端决定实际售价。通过检查后，商品名称、规格和单价作为快照交给订单服务保存。R（结果）当前实现能够拒绝金额变化并保留成交时的信息，相关单元测试覆盖了价格变化和快照内容。库存查询仍只是提前检查，真正能否预占由库存服务的原子更新决定，不能因为结算查询有货就承诺一定下单成功。

#### Q05｜结算如何保证重复提交不重复下单，结果未知时怎么办？

口播：S（情境）双击、断网重试或响应丢失都可能让同一次结算到达后端多次，简单禁止按钮连点并不能约束这些请求。T（任务）我会说明系统如何识别同一次操作，并区分明确失败和结果未知。A（动作）前端按账号保留原键与原确认金额，服务端先查已有意图；只有新键才重新校验商品，并在一次插入中固定订单号与完整快照。唯一约束负责并发认领，冲突后原金额相同就重放获胜意图，不同则拒绝。即使后来购物车变了，也不会用新商品替换原订单；摘要虽有保存，当前并不据此重新比较购物车。远端异常或补偿中仍保留未知事实。R（结果）当前实现能在清车、改价和响应丢失后继续核对原单，并通过有限恢复推进状态；超限或历史信息不足仍需人工核对。我不会把防止重复创建扩大成任何失败都能自动完成。

#### Q06｜系统怎样确认用户身份，并防止查看或操作他人的订单？

口播：S（情境）浏览器里的用户编号和请求头都可以被修改，前端隐藏按钮也不能阻止别人直接调用接口。T（任务）我会把可信身份和资源归属分开解释：先确定调用者是谁，再判断这张订单是否属于他。A（动作）登录由后端校验密码并签发 JWT，网关先删除客户端可能伪造的身份头和内部令牌，再对受保护请求验签并注入可信用户编号。商城服务还会自行验证登录令牌；订单与模拟支付入口要求内部令牌，防止绕开网关只伪造用户头。业务层随后将当前用户与订单归属比较，模拟支付也先读取订单归属再推进状态。R（结果）仓库已有直连伪造请求被拒绝和本人订单操作的验证记录，能说明这条信任边界已经加固。不过共享内部令牌不是完整的服务身份体系，也不能替代生产网络隔离、密钥轮换和细粒度权限。

#### F03｜追问：购物车用了 Redis，缓存和库存分别承担什么职责？

口播：S（情境）项目同时使用购物车缓存和库存快速预扣，名称里都有 Redis，容易误以为加入购物车已经锁住商品。T（任务）我需要先分清数据所有权，再说明缓存故障为什么不应该直接改变交易事实。A（动作）我的理解是，购物车的数据库记录才是事实，缓存只保存商品 SKU 标识、数量和勾选状态；展示价格与库存另行查询，结算还会直接读取数据库中的选择并重新校验。购物车写操作完成事务后删除缓存，读缓存失败可以回源，删除失败则可能保留一段时间的旧展示。真正的库存预占发生在订单流程，由库存服务的条件更新或另一条快速入口的原子脚本处理。R（结果）当前实现让缓存不可用时购物车仍有回源路径，但这不等于缓存强一致，也没有证明缓存带来的吞吐收益。加入购物车不保证库存，库存未知也不能简单当成售罄。

#### F04｜追问：结算记录停在结果未知时，现在能够自动恢复吗？

口播：S（情境）商城调用订单服务后可能丢失响应，远端已建单，本地却仍处理中。T（任务）恢复时必须找到原交易，不能换号重复下单。A（动作）当前先持久化订单号、完整快照及租约，再调用 Order；后台先只读核对原单，仅在允许的窗口内用原快照重试创建。窗口截止后只核对，未查到也不证明旧请求不会晚到；超限或历史缺快照转人工确认。R（结果）已用真实丢响应和 Commerce 重启验证原单恢复；原键、原金额及后来购物车修改均保留。这是有限恢复机制，不是任何故障都自动成功。

### 第 3 组：并发库存与竞争处理（高频）

#### Q07｜为什么加了事务，“先查后扣”仍然会超卖？

口播：S（情境）我会从两个请求同时读到同一个库存值讲起：它们各自计算扣减后的数字，再按绝对值写回，可能都报告成功，数据库却只体现一次扣减。T（任务）我要区分事务保证的一组操作一起提交，与竞争请求之间能否正确扣减，这不是同一个问题。A（动作）我的理解是，本地事务并没有自动把普通查询变成排他读取；当前方案把余量判断和增量扣减放进同一条更新语句，并检查影响行数，多商品再由本地事务保证整体提交。库存预占还会把可售数量转入锁定数量，供支付和取消继续处理。R（结果）仓库历史单体实验中，原子更新的超卖统计为零，但这不等于新版商城所有链路已通过相同压测；我会同时解释统计口径，而不是只展示库存没有出现负数。

#### Q08｜多个 SKU 为什么会死锁，统一排序解决了什么？

口播：S（情境）我会把死锁还原为两个订单都购买两种商品，一个先锁第一种，另一个先锁第二种，随后双方等待对方已经持有的锁。T（任务）我要说明怎样破坏这条循环等待，而不是把所有数据库等待都说成死锁。A（动作）当前库存实现先合并重复商品，再按照商品标识升序处理；预占、释放和确认都保持一致的记录访问顺序，并在本地事务中完成对应状态与数量修改。讲解时我会区分“请求传入顺序”和“实际数据库加锁顺序”，同时核对其他写入路径是否遵守约定。R（结果）仓库历史单体对照实验里，四十个事务的反向加锁场景有二十次死锁回滚，排序路径在同规模测试下全部完成。这证明了特定循环等待的改善，不代表其他索引、其他事务或更复杂业务永远不会死锁。

#### Q09｜支付与用户取消、超时关单同时发生时，以谁为准？

口播：S（情境）我会先说明同一订单可能同时收到支付回调和取消请求，定时任务也可能发现它已经到期。T（任务）核心是避免订单已经支付，另一个执行者却把有效预占释放掉，所以不能按哪个请求先进入接口来判断。A（动作）我的理解是，支付和取消都通过数据库条件更新竞争订单状态；支付还要求库存已经预占，超时关单会再次检查到期条件。取消成功后先留下待补偿事实，再请求库存释放，失败由后续扫描重试；条件更新没有成功的执行者需要尊重最新状态，不能继续盲目释放。R（结果）当前实现和对应测试覆盖了取消后晚到的预占结果、重复请求以及关单竞争失败不释放等分支。返回订单已取消只说明业务决定已经落库，不保证该响应时刻库存补偿也全部完成。

#### F05｜追问：如果释放请求比预占请求先到，怎么办？

口播：S（情境）我会用跨服务超时来解释这个问题：订单侧以为需要补偿并先发送释放，但先前的预占请求可能仍在网络中，稍后才抵达库存服务。T（任务）如果释放只查询已有锁记录，发现没有就结束，晚到预占仍可能扣库存，形成没有有效订单的占用。A（动作）当前实现会为订单建立持久化取消栅栏，并在本地事务里对这条记录加锁；即使没有任何库存锁记录，也要留下已经取消的事实。预占进入时先检查同一个栅栏，发现已经取消就拒绝，因此释放零条库存记录和没有留下状态是两回事。R（结果）对应测试验证了取消先到后拒绝晚到预占的分支。我的理解是，这解决的是特定顺序倒置问题，仍依赖数据库事务和持久化事实，不能把它概括为所有远程失败都会自动恢复。

#### F06｜追问：超卖和死锁的数据怎样统计，为什么不能直接外推？

口播：S（情境）我会先说明这些数字来自仓库保留的单体实验，不是本次重新执行，也不是新版商城生产流量。T（任务）面试里需要证明实验确实测到了问题，并交代分母、负载和成功含义。A（动作）每单一件的库存实验把成功返回数减去实际库存减少量作为超卖统计，因此绝对值覆盖即使没有产生负库存，也能被发现；一次三百并发任务、连接池上限二十的历史复验中，两种不安全方案分别记录二百四十五和二百七十八件，原子方案为零。死锁实验则按四十个事务中的死锁回滚数计算比例。R（结果）这些证据支持限定条件下的正确性对照；我不会把三百任务说成三百条同时执行的数据库连接，也不会把排序场景零死锁外推成生产系统永远没有死锁。

### 第 4 组：支付幂等与可靠投递（进阶高频）

#### Q10｜重复支付回调怎样处理，模拟支付与真实支付有什么区别？

口播：S（情境）我会先区分回调重复和用户反复点击模拟支付：前者可能来自对方重试，后者只是本地演示入口，两者都不应该重复推进订单。T（任务）需要确认请求来源、订单归属和交易身份，再决定本次是首次成功、同一交易重放还是冲突。A（动作）当前回调入口检查签名和时间窗，再交给订单服务执行条件更新；交易号还有数据库唯一约束。未更新成功时，只有订单已经支付且交易号相同才按幂等成功处理。模拟入口先检查订单属于当前用户，再使用稳定的模拟交易号复用确认链路。R（结果）当前实现及测试覆盖了重复确认、交易冲突和归属校验，但模拟支付不会产生真实扣款，支付服务也没有完整持久化支付单与回调记录。我会把这些限制明确说出，不包装成已经接入真实支付平台。

#### Q11｜为什么需要本地消息表，它为什么是至少一次而不是恰好一次？

口播：S（情境）我会从“订单已经改成已支付，但调用库存确认时进程退出”这个窗口讲起，仅靠请求线程往下执行会丢失后续工作。T（任务）目标是把需要确认库存这件事变成可恢复的持久化事实，同时避免声称一个事务能覆盖两个服务。A（动作）当前实现把订单支付状态和待发送事件写在同一个本地事务中，随后由定时任务领取事件，通过内部请求调用库存，成功后才记录已经发送。库存端用锁定记录的条件状态迁移处理重复确认；调用失败则按当前发布器规则重试或进入死信。R（结果）这样即使请求处理结束，后续工作仍有数据库依据；但库存已确认而发送标记尚未提交时仍会重放，所以是至少一次尝试配合消费幂等，不是恰好一次投递，也不意味着进入死信后业务会自动完成。

#### Q12｜远程调用超时为什么不能直接当成失败，系统怎样恢复？

口播：S（情境）超时可能表示未收到请求、执行中，或者已提交但响应丢失，调用方无法区分。T（任务）要保留未知事实，不能随意释放有效订单的库存。A（动作）普通创建先持久化订单意图；未知结果先用条件更新赢得取消决策，再幂等释放，失败保留待补偿。新增扫描器识别超过宽限的普通 RESERVING，排除 Redis 命令拥有的订单；库存取消栅栏拦住晚到预占。R（结果）已验证预占前和预占后中断 Order 均自动收敛。取消 CAS 未赢时只读取当前状态，不释放 RESERVED/PAID；Redis 链路仍按独立阶段处理，不能套用普通补偿。

#### F07｜追问：库存确认成功但事件没标记已发，会怎样？多实例安全吗？

口播：S（情境）库存已确认，但发布器在记录发送成功前退出，事件仍是处理中。T（任务）既要恢复投递，又不能重复扣减或让旧执行者覆盖新结果。A（动作）每次领取生成独立 owner，使用数据库时钟的 60 秒租约；写回同时检查状态、owner 和租约未过期，过期领取重新排队。库存端只在 LOCKED 到 CONFIRMED 的条件迁移成功时消耗锁定库存。R（结果）这解释了至少一次投递、消费幂等和旧执行者栅栏的组合；单实例恢复证据不等于多实例真实竞争已验证。

#### F08｜追问：所有重试任务都有租约、指数退避和死信吗？

口播：S（情境）订单补偿、支付事件和异步下单拥有不同事实与失败阶段，不能用一套口径概括。T（任务）逐类核验领取、退避、终止条件及是否允许归还库存。A（动作）普通补偿仍是扫描待补偿订单，没有统一的 owner 租约、退避上限和死信。支付事件已有 owner/lease、陈旧恢复、指数退避及第十次失败转死信；受控脚本核验已支付事实、预期状态与重驱代数后重新排队并记录审计。异步命令则保留自己的状态机。R（结果）能力仍不齐整；重驱成功只证明重新排队，必须继续核对事件已发送和库存已确认，告警与生产运维后台仍待完善。

### 第 5 组：异步性能与验证边界（进阶）

#### Q13｜Redis 异步下单为什么需要持久化命令和 Lua？返回 202 代表什么？

口播：S（情境）我理解这个快速入口要缩短请求等待时间，但把后续处理放到后台，也增加了进程退出和远程结果未知的风险。T（任务）我会先讲清受理、预扣、订单落库三个时刻，不能用一个成功响应掩盖中间状态。A（动作）微服务路径先保存完整命令，再调用库存服务；库存通过 Lua 校验全部商品后原子预扣，并利用订单号和商品签名识别重复或冲突请求。后台随后凭租约领取任务，复用普通订单创建流程，再推进库存账本和命令状态。预扣确认未知时保留事实等待重试，不假设远端没有执行。R（结果）当前实现返回 202 只代表已受理，甚至可能尚未确认预扣，不能向用户承诺订单已创建。这个入口也不是当前商城购物车结算的必经路径，我会明确区分已有能力与实际业务接入范围。

#### Q14｜Redis 库存和 MySQL 不一致时，如何判断是正常在途还是异常？

口播：S（情境）我的理解是，Redis 先预扣而数据库稍后落库时，两边库存短时间不同并不一定是错误，直接比较会把正常异步过程误报为异常。T（任务）我会先说明对账需要解释在途数量，同时保持各服务的数据所有权，不能让库存服务绕过接口读取订单私有表。A（动作）当前库存服务维护自己的预扣账本，以数据库可售库存减去账本中仍待落库的预扣量，得到期望的 Redis 数值，再报告差异、缺失库存键和损坏账本。另一个定时任务统计积压、最老任务年龄及状态数量，更新内存指标供监控采集；完整对账仍由独立请求触发。R（结果）当前实现能够提供排查信号，但只报告、不自动改库存。本地数据库事务不构成跨 Redis 的全局快照，活跃写入仍可能出现短暂差异；指标刷新失败会保留旧值，因此我不会仅凭数值不变判断系统正常。

#### Q15｜怎样说明压测的性能收益？距离生产落地还缺什么？

口播：S（情境）已有记录比较了同机单体的同步下单和 Redis 异步受理，后者返回得更快，但两个接口确认的完成阶段不同。T（任务）我会把性能结论限定在相同实验条件和入口响应范围，避免把受理吞吐当成最终订单处理能力，更不会当作新版商城的生产容量。A（动作）读报告时，我会核对线程数、持续时间、预热、三轮中位数和错误率，还会看命令是否处理完、订单与库存是否收敛。后续生产化需要先补齐真实支付单及回调记录、人工确认处置，再验证多实例竞争、服务重启与依赖故障，并完善网络隔离、告警和人工恢复流程。R（结果）历史短时实验记录的吞吐中位数为每秒 113.30 与 342.91 次，只能支持当时异步受理响应能力更强。本次没有重跑这些实验，当前项目也没有上述生产验证，因此我会把它介绍为有恢复机制和验证证据的本机项目，而非已上线的高可用商城。

#### F09｜追问：订单已经创建，但库存账本物化失败，为什么不能直接回补？

口播：S（情境）我会把这个故障拆成两个事实：订单和数据库库存预占可能已经成功，只是后续把预扣账本标为已落库时超时或失败。T（任务）此时首要任务是保住有效订单对应的库存，不能把一个辅助状态推进失败等同于整个下单失败。A（动作）我的理解是，处理方应保留持久化命令，通过退避继续重试，并复用订单创建及账本更新的幂等边界。当前实现也区分了预扣准备阶段和订单落库阶段；后者遇到未知结果或账本更新失败，不会因为达到某个固定重试次数就回补。只有确认订单进入明确失败终态，才尝试对应补偿。R（结果）这样避免了有效订单仍占用商品、Redis 却提前恢复可售数量的错误，但持续重试并不意味着任何故障都会自动恢复。我会结合积压和最老任务年龄观察是否长期停滞，再核对订单与库存事实，而不是看到异常日志就直接加回库存。

#### F10｜追问：单体限流能否算新版商城全局限流？现有压测脚本能直接复用吗？

口播：S（情境）我的理解是，仓库保留了单体实验和新版商城两套运行路径，相同技术名称不代表已经覆盖相同入口。T（任务）我会先核对限流过滤范围与压测请求契约，避免把旧实验能力直接写成商城的线上保护措施。A（动作）现有单体限流默认关闭，只匹配配置的 Redis 下单路径，先申请全局桶再申请接口桶，两次脚本各自原子，但不是双桶整体事务，也没有迁入新版网关。旧压测脚本只执行一轮并生成报告，不会自动预热、重置数据库或确认异步任务全部完成；默认端口虽是 18080，请求却没有新版登录凭证及所需价格字段。R（结果）因此我不会直接运行默认参数宣称商城压测通过。需要先选择正确运行路径、适配认证与请求内容，在专用数据环境安排预热和多轮，再独立核验最终状态，才能得到范围明确、可比较的结果。

## 4. 源码证据索引

索引将正文的通用表述映射到当前实现；这里出现的方法名和状态名用于回到源码核对，不需要在面试开场全部背出。

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 项目范围与验收层级 | [docs/project-function-boundary.md](docs/project-function-boundary.md)；[docs/v2-order-actions-evidence-2026-09-24.md](docs/v2-order-actions-evidence-2026-09-24.md)：五进程、模拟支付、119 项历史测试及尚未验证边界 | 简介；Q01、F01、Q15 |
| 普通商城下单入口 | [CheckoutController.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/web/CheckoutController.java) 的 `submit`，实际路径 `POST /api/checkout`；[CheckoutService.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/service/CheckoutService.java) 的 `submit → toOrderRequest`；[OrderClient.java](microservices/fulfillment-api/src/main/java/com/why/fulfillment/api/order/OrderClient.java) 的 `create` | Q02 |
| 服务划分与事务边界 | [microservices/pom.xml](microservices/pom.xml) 的 `modules`；[OrderRepository.java](microservices/order-service/src/main/java/com/why/fulfillment/order/repository/OrderRepository.java) 的 `insertPending`；[InventoryReservationServiceImpl.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/service/impl/InventoryReservationServiceImpl.java) 的 `reserve`；跨服务调用在 [OrderApplicationService.java](microservices/order-service/src/main/java/com/why/fulfillment/order/service/OrderApplicationService.java) 的 `createPending` | Q03、F02 |
| 商品重校验和快照 | [CheckoutService.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/service/CheckoutService.java) 的 `revalidate / sum / requireAmountUnchanged`；[OrderApplicationService.java](microservices/order-service/src/main/java/com/why/fulfillment/order/service/OrderApplicationService.java) 的 `createFromCommerce / requireTotalMatchesItems`；[OrderCreateFromCommerceTest.java](microservices/order-service/src/test/java/com/why/fulfillment/order/service/OrderCreateFromCommerceTest.java) 的 `snapshotIsPersistedWithTheOrderLine` | Q04 |
| 结算认领与未知结果 | [CheckoutService.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/service/CheckoutService.java) 的 `digest / claimIdempotencyKey / requireSameIntentAmount`；[CheckoutRecoveryService.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/service/CheckoutRecoveryService.java) 的 `prepare / replay / recover`；[CheckoutRequestMapper.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/checkout/mapper/CheckoutRequestMapper.java) 的 `insertClaim / finishSubmitted / finishRejected`；[CheckoutServiceTest.java](microservices/commerce-service/src/test/java/com/why/fulfillment/commerce/checkout/service/CheckoutServiceTest.java) 的 `anOrderServiceFailureLeavesTheClaimInProgress / aReplayStillWorksAfterSuccessfulCheckoutClearsTheCart` | Q05、F04 |
| 购物车缓存与事实 | [CartItemSnapshot.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/cart/service/CartItemSnapshot.java)：`skuId / quantity / selected`；[CartCache.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/cart/service/CartCache.java) 的 `read / write / evictAfterTransaction`，使用 `afterCompletion`；[CartService.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/cart/service/CartService.java) 的 `selectedItems / assemble` | F03、Q04 |
| 身份与订单归属 | [AuthenticationGlobalFilter.java](microservices/gateway/src/main/java/com/why/fulfillment/gateway/auth/AuthenticationGlobalFilter.java) 的 `filter`；[CurrentUserArgumentResolver.java](microservices/commerce-service/src/main/java/com/why/fulfillment/commerce/auth/CurrentUserArgumentResolver.java) 的 `resolveArgument`；[InternalServiceTokenFilter.java](microservices/order-service/src/main/java/com/why/fulfillment/order/web/InternalServiceTokenFilter.java)；[OrderApplicationService.java](microservices/order-service/src/main/java/com/why/fulfillment/order/service/OrderApplicationService.java) 的 `findOwned / cancelOwned` | Q06 |
| 先查后扣与原子 SQL | 单体 [StockServiceImpl.java](src/main/java/com/why/fulfillment/inventory/service/impl/StockServiceImpl.java) 的 `deductNaive / deductNaiveInTransaction / deductAtomic`；[SkuStockMapper.xml](src/main/resources/mapper/SkuStockMapper.xml) 的 `updateStockAbsolute / reduceStockAtomic`；[StockConcurrencyTest.java](src/test/java/com/why/fulfillment/inventory/StockConcurrencyTest.java) 的 `race / oversold` | Q07、F06 |
| SKU 排序与死锁证据 | 微服务 [InventoryReservationServiceImpl.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/service/impl/InventoryReservationServiceImpl.java) 的 `normalize`；单体 [Cycle4DeadlockIntegrationTest.java](src/test/java/com/why/fulfillment/inventory/Cycle4DeadlockIntegrationTest.java) 的 `reverseLockOrderShouldReproduceDeadlock / reverseInputOrderShouldCompleteWithoutDeadlock`；[周期 4 证据](docs/cycle4-evidence-2026-09-19.md)，另一次超卖复验见 [README.md](README.md) | Q08、F06 |
| 支付与取消竞争 | [OrderRepository.java](microservices/order-service/src/main/java/com/why/fulfillment/order/repository/OrderRepository.java) 的 `markPaidIfPending / markUserCanceledForCompensation / markExpiredForCompensation`；[OrderExpirationScheduler.java](microservices/order-service/src/main/java/com/why/fulfillment/order/task/OrderExpirationScheduler.java) 的 `closeExpiredOrders`；[OrderApplicationServiceTest.java](microservices/order-service/src/test/java/com/why/fulfillment/order/service/OrderApplicationServiceTest.java) 的 `reserveFinishingAfterCancelNeverReportsReserved / duplicateSuccessfulReservationNeverReleasesValidOrder` | Q09 |
| 取消栅栏 | [InventoryReservationFenceMapper.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/mapper/InventoryReservationFenceMapper.java) 的 `ensureExists / selectStatusForUpdate / updateStatus`；[InventoryReservationServiceImplTest.java](microservices/inventory-service/src/test/java/com/why/fulfillment/inventory/InventoryReservationServiceImplTest.java) 的 `cancellationFenceRejectsReserveThatArrivesAfterRelease` | F05、Q12 |
| 支付回调和模拟支付 | [PaymentCallbackSignatureVerifier.java](microservices/payment-service/src/main/java/com/why/fulfillment/payment/service/PaymentCallbackSignatureVerifier.java) 的 `verify`；[PaymentApplicationService.java](microservices/payment-service/src/main/java/com/why/fulfillment/payment/service/PaymentApplicationService.java) 的 `mockSuccess / acceptSuccess`；[PaymentApplicationServiceTest.java](microservices/payment-service/src/test/java/com/why/fulfillment/payment/service/PaymentApplicationServiceTest.java) 的 `mockPaymentRequiresOrderOwnership / mockPaymentUsesStableTradeNumberForRetries`；交易号约束见 [schema 隔离迁移](microservices/sql/migration-cycle11-split-schema.sql) 的 `uk_order_out_trade_no` | Q10 |
| 本地事件与重复确认 | [OrderRepository.java](microservices/order-service/src/main/java/com/why/fulfillment/order/repository/OrderRepository.java) 的 `markPaidIfPending`；[ConfirmationOutboxRepository.java](microservices/order-service/src/main/java/com/why/fulfillment/order/repository/ConfirmationOutboxRepository.java) 的 `claimConfirmationEvent / recoverStaleConfirmationClaims / retryConfirmation`；[InventoryConfirmationPublisher.java](microservices/order-service/src/main/java/com/why/fulfillment/order/task/InventoryConfirmationPublisher.java) 的 `publishReady / publish`；[SkuStockLockMapper.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/mapper/SkuStockLockMapper.java) 的 `markConfirmedIfLocked` | Q11、F07、F08 |
| 普通远程未知与补偿 | [OrderApplicationService.java](microservices/order-service/src/main/java/com/why/fulfillment/order/service/OrderApplicationService.java) 的 `compensateUnknown / retryPendingCompensation / resultForExisting`；[OrderCompensationScheduler.java](microservices/order-service/src/main/java/com/why/fulfillment/order/task/OrderCompensationScheduler.java) 的 `retryPending`；普通 `RESERVING` 与 `PENDING_COMPENSATION` 不能混为同一恢复入口 | Q12、F08 |
| 异步受理、租约与 Lua | [RedisOrderApplicationService.java](microservices/order-service/src/main/java/com/why/fulfillment/order/service/RedisOrderApplicationService.java) 的 `accept / prepare / processReady`；[RedisOrderCommandRepository.java](microservices/order-service/src/main/java/com/why/fulfillment/order/repository/RedisOrderCommandRepository.java) 的 `insertPreparing / claim / markSucceeded`；[RedisOrderCommandScheduler.java](microservices/order-service/src/main/java/com/why/fulfillment/order/task/RedisOrderCommandScheduler.java)；[redis_reserve_once.lua](microservices/inventory-service/src/main/resources/lua/redis_reserve_once.lua) | Q13、F08 |
| 账本物化失败不能乱回补 | [RedisInventoryCoordinator.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/redis/RedisInventoryCoordinator.java) 的 `reserve / materialize`；[InventoryRedisLedgerService.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/redis/InventoryRedisLedgerService.java) 的 `recordPending / recordMaterialized`；[RedisOrderCommandSchedulerTest.java](microservices/order-service/src/test/java/com/why/fulfillment/order/task/RedisOrderCommandSchedulerTest.java) 的 `projectionPendingSchedulesRetryWithoutCompensationEvenAtRetryLimit / unknownReadyFailureNeverCompensatesBecauseOrderMayAlreadyExist` | F09、Q13 |
| 只读对账与指标刷新 | [StockReconciliationService.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/reconciliation/StockReconciliationService.java) 的 `inspect`；[InventoryServiceMetrics.java](microservices/inventory-service/src/main/java/com/why/fulfillment/inventory/metrics/InventoryServiceMetrics.java) 的 `refresh / recordReconciliation`；[OrderServiceMetrics.java](microservices/order-service/src/main/java/com/why/fulfillment/order/metrics/OrderServiceMetrics.java) 的 `refresh` | Q14、F09 |
| 压测口径与脚本限制 | [performance/README.md](performance/README.md)、[周期 5 验收记录](docs/cycle5-evidence-2026-09-19.md)：10 线程、2 秒升压、5 秒持续、三轮中位数；[run-cycle5-jmeter.ps1](performance/run-cycle5-jmeter.ps1)、[cycle5-order-comparison.jmx](performance/cycle5-order-comparison.jmx)：单轮执行、HTTP 断言、旧请求契约 | Q15、F10 |
| 限流生效范围 | 单体 [OrderRedisRateLimitFilter.java](src/main/java/com/why/fulfillment/ratelimit/OrderRedisRateLimitFilter.java) 的 `shouldNotFilter / doFilterInternal`；[RedisTokenBucketServiceImpl.java](src/main/java/com/why/fulfillment/inventory/redis/RedisTokenBucketServiceImpl.java) 的 `tryAcquire`；[token_bucket.lua](src/main/resources/lua/token_bucket.lua)；[单体配置](src/main/resources/application.yml) | F10 |

证据使用规则：Mock 测试说明受控条件下的调用和状态分支；真实数据库实验说明对应环境中的锁竞争；历史 HTTP 记录说明那次联调的结果。源码存在不证明多实例能力，测试文件存在也不证明本次执行通过，更不能单凭提交记录推定个人独立贡献。
