# 代码审计整改记录（2026-09-19）

对照 `D:\vibecoding\履约中台-代码审计-20260919.md` 核验。原审计生成时项目尚未安装
JDK、Maven 和 Redis；以下状态以整改后的代码和真实运行结果为准。

## 已关闭的高优先级项

- 项目已使用 JDK 17.0.20.1、Maven 3.9.16 编译并运行测试。
- 周期 4 已补未排序死锁基线、按 `skuId` 排序的生产路径对照和 InnoDB 死锁现场。
  20 轮共 40 个未排序事务产生 20 个死锁牺牲者；排序后 40 个事务全部成功。
- 周期 1 已增加带事务的先查后扣对照。实测证明本地事务没有消除覆盖写超卖。
- 支付回调已增加真实 MySQL 双线程测试：两个相同交易号的并发回调都返回成功，
  订单只产生一条 outbox 事件。
- 外部交易号若已属于另一订单，由唯一索引触发并转换成明确的业务冲突响应。
- Redis 已用真实应用链路验收：HTTP 创建 2 秒超时订单后，订单自动取消并完整释放库存。
- 超时关单消费者增加有限重试、退避、日志和死信处理，并补单元测试。
- 已增加下单、支付成功回调和库存查询 HTTP 入口，满足周期 5 压测前置条件。
- SQL 标准输出默认关闭，业务日志降为 info；并发测试单独记录线程数和连接池上限。
- 修正 `count` 标识符引用、库存 CHECK 注释和 `version` 字段用途说明。

## 周期 5 补充整改

- 已增加 Redis Lua 多 SKU 原子预扣、幂等补偿和真实 Redis 并发测试。
- 已增加可靠异步命令表、数据库租约、有限重试、死信补偿与完整 HTTP 入口。
- 已增加库存差异清单，对账时扣除尚未落库的 Redis 预扣量。
- 已增加全局与接口级 Lua 令牌桶，限流命中返回 HTTP 429。
- 已安装并验证 JMeter 5.6.3；纯 MySQL 与 Redis 路径完成预热及三轮同配置测试。
- 原始结果和口径说明见 `docs/cycle5-evidence-2026-09-19.md`。

## 已明确的后续边界

- `sku_stock.version` 是乐观锁教学对照的预留字段，不作为当前生产路径字段。
- outbox 的 5 分钟租约与多实例抢占属于部署扩展设计；当前没有宣称已验证多实例。
- 单体按业务包组织。周期 7 拆出跨进程运行切片后，本地事务不再覆盖远程调用，已改用显式状态与补偿；剩余单体能力仍需逐项迁移。
- 关单消费者默认理论上限为 200 单/秒；独立容量与积压测试仍需在长时间压测中验证。
- 当前多键 Lua 面向单 Redis 实例；迁移 Redis Cluster 时需要统一 hash tag 或重新设计分槽补偿。

## 证据

- `docs/cycle4-evidence-2026-09-19.md`
- `docs/cycle4-innodb-deadlock-2026-09-19.txt`
- `docs/redis-timeout-e2e-2026-09-19.md`
- Maven Surefire 测试报告目录：`target/surefire-reports`

周期 5 最终全量回归：46 个测试，0 failures，0 errors，0 skipped，`BUILD SUCCESS`。

## 周期 7 补充整改（2026-09-21）

- 已增加独立 Gateway、Order、Inventory、Payment 进程和只含 DTO/Feign 契约的 API 模块。
- 订单与库存跨进程后改用预占状态机和幂等补偿任务，不再依赖单体本地事务回滚远程操作。
- 支付仅允许命中“待支付且库存已预占”订单；支付状态与库存确认事件同事务写入订单本地 Outbox。
- Outbox 使用条件抢占、陈旧任务回收和指数退避；库存确认以锁定记录状态条件更新保持幂等。
- 真实启动发现并修复 OpenFeign 禁止接口类级 `@RequestMapping` 的问题，新增契约解析测试覆盖启动期约束。
- Reactor 共 20 项测试通过；Gateway 主链路、重复支付、Outbox 投递和库存服务宕机后的自动补偿均已实测。
- 审计发现的“释放早于预占提交”竞态已通过数据库取消栅栏关闭；新增对应回归测试。
- 库存变更接口改为内部令牌保护，支付回调增加时间窗与 HMAC 签名，外部错误不再返回下游地址和异常文本。
- Feign 增加显式连接/读取超时，Outbox 达到最大重试次数后进入死信；迁移脚本增加历史状态回填并完成两次连续执行验证。

证据见 `docs/cycle7-microservices-evidence-2026-09-21.md`。当时尚未关闭的边界包括 Nacos 服务发现、数据库物理拆分、
多实例故障注入，以及把超时关单和 Redis 快速下单从单体迁入独立服务；其中超时关单已在周期 9 完成。

## 周期 8 补充整改（2026-09-21）

- 订单主表与 `order_item` 明细改为 Order Service 同一本地事务写入。
- 同一 `orderId` 重放时比较完整业务载荷，同载荷返回已有状态，异载荷返回 409。
- `RESERVING` 状态的重复请求会重新调用库存幂等预占接口，用于恢复订单已提交但远程调用未完成的窗口。
- `order_item` 增加 `(order_id, sku_id)` 唯一约束，迁移脚本连续执行两次成功。
- 微服务 Reactor 共 26 项测试通过；真实 Gateway 联调验证首次创建、同载荷重放、异载荷冲突和明细查询。

证据见 `docs/cycle8-order-idempotency-evidence-2026-09-21.md`。普通单体下单入口仍未迁入该幂等语义；并发重复请求的真实数据库竞争和独立幂等键仍属于后续边界。

## 周期 9 补充整改（2026-09-21）

- 微服务 Order 增加持久化到期时间和联合索引扫描，服务重启后可从订单表恢复未处理关单。
- 关单先通过条件 UPDATE 抢占状态，再复用库存补偿释放；远程释放失败不会丢失待处理事实。
- 支付与关单均使用订单状态和预占状态条件更新，只有一个分支能够成功。
- 增量迁移连续执行两次成功；2 秒真实关单恢复库存，支付成功订单超过期限后未被误取消。
- 微服务 Reactor 共 28 项测试通过。
- 初始化脚本与周期 7 至 9 迁移在临时数据库连续执行成功；单体旧字段写入兼容性验证通过，单体 47 项测试全部通过。

证据见 `docs/cycle9-timeout-close-evidence-2026-09-21.md`。数据库扫描容量、百万级积压、多实例故障注入和严格截止时间语义仍属于后续边界。

## 周期 10 补充整改（2026-09-21）

- Redis 快速入口迁入 Gateway 和 Order，先持久化命令再远程预扣，移除了“Redis 已扣但可靠事实尚未落库”的孤儿窗口。
- Inventory 统一拥有 Redis Lua、库存键、载荷签名和取消墓碑；补偿先到时，晚到预扣会被拒绝。
- 后台异步落库增加租约抢占、过期恢复、指数退避和终态补偿，商品单价完整保存在命令中。
- 超时关单释放 MySQL 后执行 Redis 条件补偿；真实验收发现并修复了只读事务中 `SELECT FOR UPDATE` 导致的 500。
- 对账采用只读报告，不自动修复；Inventory 读取共享命令表被记录为数据库物理拆分前的技术债。
- 周期 10 后微服务 Reactor 共 37 项测试通过；真实 Gateway 验证预扣、异步落库、到期双存储补偿和一致对账。

证据见 `docs/cycle10-redis-microservice-evidence-2026-09-21.md`。Redis Cluster、多实例租约故障注入、命令表分库后的事件投影、自动修复和微服务容量数据仍未验证。

## 周期 11 补充整改（2026-09-21）

- Order 与 Inventory 分别迁入 `fulfillment_order` 和 `fulfillment_inventory`，并使用只具备自身 schema `SELECT/INSERT/UPDATE` 的应用账号。
- 实测两个应用账号均无法读取对方 schema，Inventory 对账代码已删除对 `microservice_order_command` 的依赖。
- 新增 Inventory 自有 Redis 预扣账本和物化接口，对账只聚合 `PENDING` 账本；补偿和物化使用条件状态转换，避免并发状态倒退。
- 修复 READY/PROCESSING 未知异常走错误 Redis 补偿的问题：只有明确失败终态才补偿；物化失败或无法判断订单是否已形成时持续退避重试，不补偿可能有效的订单。
- 共享库和拆库迁移均回填 READY/PROCESSING 命令，并把含价格的 Order JSON 规范化为库存字段；脚本连续执行两次成功。
- 微服务 Reactor 44 项测试和单体 47 项测试通过；真实双 schema 四进程链路完成预扣、异步落库、超时双补偿和目标 SKU 一致对账。

证据见 `docs/cycle11-schema-isolation-evidence-2026-09-21.md`。当前仍共享一个 MySQL 实例；账本积压告警、多实例故障注入、服务发现和分布式追踪尚未完成。
## 周期 12 补充整改（2026-09-21）

审计确认周期 0–11 的主要交付均有源码、自动化测试或真实联调证据，边界说明已明确：当前仍是单 MySQL 实例的双 schema，消息为至少一次投递，尚未完成服务发现、多实例无重复证明和生产级高可用。

本周期补齐微服务可观测性缺口：四个服务均接入 Prometheus registry 并纳入 `ops/prometheus.yml`；Order 统计 Redis 命令与支付确认 Outbox 状态，Inventory 统计 Redis 账本积压、最老积压年龄和最近一次对账差异。指标刷新采用定时任务和原子值，避免在 Prometheus 请求线程内查询数据库。

未宣称的内容：本周期没有新增真实多实例故障注入，没有实现告警路由或人工补偿接口，也没有把静态服务地址改成 Nacos。上述内容保留为后续周期边界。
