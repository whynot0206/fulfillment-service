# 复制为 .env.local 后按需修改。.env.local 不进版本库。
#
# 只有 VITE_ 前缀的变量会被 Vite 读到。注意：以 VITE_ 开头的变量会被打进
# 前端产物，任何访问者都能在浏览器里看到它——所以这里永远不能放密钥、令牌、
# 数据库口令。前端没有"服务端环境变量"这回事。
#
# Gateway 地址，开发服务器把 /api 代理到这里。默认 http://localhost:18080
VITE_GATEWAY_URL=http://localhost:18080
