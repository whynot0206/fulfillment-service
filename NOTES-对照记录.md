# 对照记录

> 规则：**先自己写完，再打开 mall4cloud 对照。** 顺序反了就只是抄。
> 这个文件是本项目最不可替代的部分——代码谁都能写，这些差异只有你有。
> 面试讲项目时，"我和工业实现的差异在哪、我怎么判断的"比"我用了什么技术栈"有说服力得多。

参照对象：`D:\vibecoding\java-high-concurrency-selector\.repo-source-cache\gz-yami__mall4cloud`
（commit `a9bb40d`，AGPLv3。本项目仅参考其设计，未拷贝代码。）

---

## 模板

```
## YYYY-MM-DD  主题

**我的写法**

**mall4cloud 的写法**（附 file:line）

**差异**

**谁更好，为什么**
（允许结论是"它更好"，也允许是"它有问题"。后者要能说出问题在什么条件下才暴露。）

**我当时没想到的是**
（这一栏最值钱，别空着。）
```

---

## 待对照清单

第 1 周：库存原子扣减 SQL —— 对照 `mall4cloud-product/src/main/resources/mapper/SkuStockMapper.xml` 的 `reduceStockByOrder`

第 1 周：库存三态字段设计 —— 对照 `sku_stock` 表的 stock / lock_stock 划分

第 3 周：下单主链路的事务边界 —— 对照 `mall4cloud-order/.../OrderServiceImpl.java:89-109`，特别注意它在事务内发 MQ 这件事（`:109`），想清楚事务回滚了消息已经发出去会怎样

第 4 周：多 SKU 扣减的加锁顺序 —— 对照 `mall4cloud-product/.../SkuStockLockServiceImpl.java:97,102`，循环内逐 SKU 扣减，未排序

第 5 周：限流 —— mall4cloud 全项目没有限流实现，这一条是"对照出了空白"，也算差异

第 4 周：多 SKU 死锁复现与修复 —— 本项目的 `InventoryReservationServiceImpl` 在扣减前合并重复 SKU，并按 `skuId` 升序处理。2026-09-19 已在 MySQL 8.0.46 实测：未排序死锁率 50%（20/40），排序后 0%（0/40）。

---

## 记录

## 2026-09-18 库存原子扣减

**我的写法**

`reduceStockAtomic` 使用一条 `UPDATE`：同时减少 `stock`、增加
`lock_stock`，并在 `WHERE` 中要求当前可售库存不少于本次扣减量。Mapper
返回 0 时，`StockService` 返回 `false`，表示库存不足；SQL 执行异常则向
上抛出，由调用方按系统异常处理。

为了让并发测试复现问题，`deductNaive` 保留了先查询、在 Java 中判断、再按
绝对值写回的流程。两个请求可能读到同一库存，并都返回成功，但后一次绝对值
写入覆盖前一次结果，因此已售数量会大于实际扣减数量。

**mall4cloud 的写法**（`mall4cloud-product/src/main/resources/mapper/SkuStockMapper.xml:89`）

`reduceStockByOrder` 同样在单条更新中完成 `stock - count`、`lock_stock + count`
和库存条件判断。其库存锁定服务检查受影响行数，小于 1 时抛出库存不足异常。

**差异**

本项目把单 SKU 扣减封装为返回布尔值的最小接口，测试可直接统计成功与失败；
mall4cloud 在一个订单事务中逐个处理 SKU，并同时维护 SKU、SPU 和库存锁定记录。
两者的原子扣减核心相同，但本项目尚未进入多 SKU 与订单状态流转范围。

**谁更好，为什么**

周期 1 阶段本项目的实现更适合验证单行条件更新的正确性，边界小且结果易观察。
真实下单链路需要 mall4cloud 那样的锁定记录和事务编排；其逐个 SKU 扣减的顺序
周期 4 的 `Cycle4DeadlockIntegrationTest` 使用真实 MySQL 双事务：未排序探针通过屏障保证两个事务各持有一把 SKU 行锁，再反向申请第二把锁；正式服务对相反输入顺序统一排序后再扣减。实测死锁率从 50% 降为 0%。

**我当时没想到的是**

超卖不一定表现为负库存。先查后扣采用绝对值写回时，库存可能仍是非负数，
但多个成功订单会覆盖成一次实际扣减；因此测试必须用“成功订单数减实际扣减量”
计算超卖件数。

## 2026-09-18 库存三态与超时关单

**我的写法**

订单创建和库存预占放在同一个本地事务中：先按 `skuId` 排序执行条件扣减，
再写入 `sku_stock_lock(status=1)`。事务提交后才把订单 ID 放入 Redisson
延迟队列。任务到期时执行 `status=1 -> status=3` 的条件更新，只有真正取消
成功才释放锁定库存；重复任务、已支付订单和已取消订单都不会重复释放。

支付确认使用同样的状态条件更新，并把 `status=1` 的锁定记录改为确认状态，
同时减少 `lock_stock`。释放和确认竞争同一条锁定记录时，数据库条件更新只允许
其中一个路径成功。

**mall4cloud 的写法**

mall4cloud 已有 SKU 可售库存、锁定库存和独立锁定记录的模型；下单后直接发送
RocketMQ 延迟消息，消费者再根据订单状态释放库存。本项目保留三态数据模型，
将延迟任务改为 Redisson 延迟队列，以减少周期 2 的中间件依赖，并把“事务提交后
才入队”作为本地实现的边界。

**差异**

本项目的关单消费者与订单、库存使用同一个数据库事务，适合当前单体阶段；
Redisson 队列只负责延迟触发，任务消费失败时重新入队，不能替代后续周期的本地
消息表和跨服务补偿。支付回调与关单的最终裁决点是订单状态条件更新，而不是
先查询再决定。

**谁更好，为什么**

周期 2 选择 Redisson 是为了用较少的中间件把状态机跑通；mall4cloud 的 RocketMQ
方案更适合已经运行消息基础设施的微服务部署。两者都必须让消费端复查订单状态，
否则支付和超时任务同时到达时会出现错误释放。

**我当时没想到的是**

延迟任务不能在订单事务提交前发送。若事务回滚而任务已经入队，消费者虽然可以
通过状态检查避免错误关单，但会产生无效任务；周期 2 用事务提交事件把这个窗口关掉，
后续仍需处理进程在提交后、入队前崩溃的极端情况。

## 2026-09-18 支付回调与本地消息表

**我的写法**

支付回调先用 `status=待支付` 条件更新订单，并把 `PAYMENT_CONFIRMED` 事件和订单
状态放在同一个本地事务中写入 `order_outbox_event`。重复回调不再重复插入事件：
如果订单已经是已支付且外部交易号相同，直接按幂等成功返回。发布器用状态条件抢占
消息，调用库存确认后标记已发送；失败按指数退避重试。

**mall4cloud 的写法**

mall4cloud 的支付回调先更新支付单，再通过 RocketMQ 通知订单和库存，当前源码没有
可见的本地消息表或事务消息闭环，存在数据库更新成功但消息未发出的窗口。

**差异**

本项目用本地消息表换取投递可靠性，消息实时性由定时发布间隔决定；发布器提供至少
一次投递，所以库存确认消费端必须幂等。当前仍是单体内接口调用，周期 3 后半段才
适合把接口替换成 Feign 服务契约。

**谁更好，为什么**

在当前单机开发阶段，本地消息表的依赖更少且能直接验证事务边界；RocketMQ 更适合
多实例部署和高吞吐异步链路，但需要额外处理 Topic、消费组、重平衡和死信队列。

**我当时没想到的是**

“重复回调返回 false”不等于幂等成功。调用方通常会把 false 当作失败而继续重试，
所以同一外部交易号已经成功落库时必须返回成功语义，同时不能再次创建下游事件。

## 2026-09-19 包边界不等于服务边界

当前 `OrderServiceImpl.createPending` 中，订单写入和库存预占依靠同一个本地事务共同
提交或回滚。按 `inventory`、`order`、`payment` 分包只是让职责清晰，并不意味着把
包直接拆成进程后仍能保持相同的一致性。

如果库存预占改成远程调用，预占成功后订单写入失败时，库存事务无法跟随订单事务
回滚。周期 6 拆服务时必须明确采用本地消息表、Saga/TCC 或补偿任务，并处理调用超时
但远端实际成功的未知结果，不能把当前的 `@Transactional` 当成跨服务事务。

## 2026-09-19 超时关单容量预判

当前消费者默认每 500ms 最多处理 100 个任务，单实例调度上限的理论值是 200 单/秒，
实际吞吐还会受单笔关单数据库事务耗时影响。周期 5 压测需要同时观察 Redis 队列长度、
超时订单滞留时长和库存释放延迟；如果开始堆积，再按实测选择提高批量、并行消费者或
拆分队列，不能只看下单接口 QPS。

## 2026-09-19 事务不能修复先查后扣

本机 MySQL 8.0.46、HikariCP 最大连接数 20、300 个并发请求的一次实测中：普通先查
后扣成功 300 次、实际扣减 55 件、超卖 245 件；给同一逻辑加本地事务后成功 300 次、
实际扣减 22 件、仍超卖 278 件；原子条件更新成功 100 次、实际扣减 100 件、超卖 0。
这些是正确性对照数字，每次调度结果会波动，结论以是否超卖及其口径为准。

## 2026-09-19 Redis 预扣与限流

**我的写法**

多 SKU 预扣和补偿各由一次 Lua 完成，订单号 marker 同时保存商品签名，重复请求可区分
真正重试与参数冲突。预扣成功后同步写可靠命令表，后台通过租约、重试和死信完成订单与
MySQL 库存事务；全局与接口级令牌桶也使用 Lua 原子更新。对账时把待落库命令计入期望值。

**mall4cloud 的写法**

当前对照版本主要依赖 MySQL 条件更新与 RocketMQ 下单链路，项目中没有可直接对照的
Redis Lua 库存预扣或统一入口令牌桶实现。

**差异**

本项目为入口削峰增加 Redis 快速路径，同时用数据库命令表关闭“Redis 已扣但任务只在内存”
的丢失窗口。代价是 202 只表示命令可靠接受，调用方需要查询最终订单状态；Redis 与 MySQL
之间仍需对账和死信补偿。

**谁更好，为什么**

同步 MySQL 路径的完成语义更强、链路更短，适合中低流量；Redis 路径在本机短时测试中入口
吞吐更高、尾延迟更低，适合需要削峰的入口，但运维与一致性成本更高。选择取决于是否真的
需要接受语义和异步落库，不能只按 QPS 决定。

**我当时没想到的是**

对账不能直接要求 Redis 等于 MySQL 可售库存。预扣已成功而命令尚未消费时，两者本来就应
相差待处理数量；忽略这部分会让正常积压全部变成假告警。多键 Lua 在单实例可原子执行，迁移
Redis Cluster 时还必须处理 key slot 约束。

## 2026-09-19 可观测性与本地展示

周期 6 没有先引入 Seata。当前代码仍依赖单体本地事务，直接加入 AT 模式无法形成公平的
跨服务对比，必须先拆出真实远程分支事务。现阶段先补 Micrometer/Prometheus 指标和运行
看板，把 QPS、P99、异步积压、死信、限流拒绝和库存差异变成可持续观察的数据。

看板轮询只读取最近一次定时对账快照；若每 5 秒都执行一次完整库存扫描，看板自身会成为
数据库压力来源。首次启动尚无快照时允许执行一次即时检查，随后由定时任务更新。

## 2026-09-21 微服务运行切片与 Saga 补偿

周期 7 没有把原来的本地 `@Transactional` 直接套到 Feign 调用上。订单先在本地记录“预占中”，
库存明确成功后改为“已预占”；远程结果未知时调用幂等释放，释放失败则进入“待补偿”，由后台任务
继续重试。这个状态机把网络超时与业务库存不足分开，避免把未知结果误记为确定失败。

支付只允许处理库存已经预占的订单。订单支付状态和 `PAYMENT_CONFIRMED` 事件在订单数据库的同一
事务提交，Outbox 发布器至少一次调用库存确认接口；库存锁定记录使用条件状态更新吸收重复调用。
Gateway、订单、库存、支付已经是独立启动进程，公共 API 模块只包含 DTO 与 Feign 契约，不共享实体
或 Mapper。当前先用可配置 URL 完成本地网络边界验证，注册中心留到运行切片完成真实端到端验收后接入。

## 2026-09-21 功能边界复核

本轮由规划、源码和风险三个视角独立审查后交叉复核，确认仓库需要按两条证据线理解：

- 根目录单体是周期 0–6 的完整实验基线，保留超时关单、Redis 快速下单、异步落库、对账、限流和业务看板。
- `microservices` 是周期 7–9 持续演进的本机四进程运行切片，迁移了订单主状态与明细、创建幂等、持久化超时关单、库存预占/释放/确认、支付回调、Outbox 和故障补偿。
- 两条路径当前共用 MySQL `fulfillment` 库。微服务使用环境变量配置的静态服务 URL，尚未接入 Nacos，也没有完成数据库物理拆分。
- 截至周期 7，`order_item` 明细持久化和普通订单创建接口幂等在两条路径中都未实现；库存预占、支付回调和 Outbox 消费只有各自的局部幂等语义。
- 周期 7 的共享内部令牌、支付 HMAC 和时间窗属于基础请求校验，不能扩展表述为生产级安全体系。
- 周期 5 的吞吐和 P99 是单机短时单体实验，不能作为微服务容量结论。

当前唯一功能矩阵和对外口径写入 `docs/project-function-boundary.md`。后续新增功能时先更新该矩阵，再更新 README 和验收证据，避免规划目标、源码现状和简历表述再次漂移。

## 2026-09-21 周期 8：订单创建幂等与明细持久化

微服务 Order 在一个本地事务中插入订单主表和规范化后的 `order_item`。订单号发生唯一键冲突时不直接把重复请求当成功，而是读取已有订单并逐项比较 `userId`、金额、SKU、SPU、数量和单价：完全一致才按幂等重放处理，任何字段不同都返回 409。

已有订单若处于终态，重放直接返回既有结果，不再调用库存；若仍处于 `RESERVING`，则重新调用 Inventory 的幂等预占接口，使“订单已提交但进程在远程调用前退出”的请求能够恢复。这个设计依赖库存侧 `orderId + skuId` 唯一键和 payload 一致性检查，不能单独依赖 Order 的唯一主键。

真实 Gateway 验收中，首次请求返回 201，相同载荷重放返回 200，修改单价后重放返回 409。数据库中只有一条订单明细和一条库存锁定记录，可售库存只减少一次。当前根目录单体的普通 MySQL 下单入口尚未同步这项能力。

## 2026-09-21 周期 9：微服务持久化超时关单

单体使用 Redisson 延迟队列在事务提交后投递订单号；微服务 Order 改为把 `timeout_seconds` 和 `expire_time` 与订单一起持久化，再按联合索引扫描到期订单。这样不需要解决“订单事务已提交但延迟任务尚未写入时进程退出”的窗口，服务重启后也能从订单表恢复任务。

扫描器不会直接远程释放后再改订单，而是先用带 `PENDING_PAYMENT + RESERVED + expire_time` 条件的 UPDATE 抢到关单权，将订单置为取消和待补偿，再调用 Inventory。释放失败不会丢失，因为后台补偿继续扫描 `PENDING_COMPENSATION`。支付回调要求同一订单仍为待支付和已预占，所以支付与关单由数据库条件更新裁决。

真实 2 秒订单约 2.64 秒完成 `CANCELED / COMPENSATED`，库存从 18/2 恢复为 20/0。另一订单在 3 秒期限前支付，等待 5 秒后仍为 `PAID`，库存锁定已确认归零。当前选择状态竞争语义：已过名义期限但尚未被扫描器领取时，支付仍可能先成功。
